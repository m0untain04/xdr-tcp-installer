# xdr-tcp-installer
Shell script installer for XDR TCP (WORKS ONLY ON UBUNTU)

# Troubleshooting
When trying to run ./start.sh you may get bad interceptor error. You can fix this by typing **sed -i -e 's/\r$//' start.sh**
